# GEP (investment) — held-out comparison (Study 3), run individually.
# Traditional baselines (k-means/k-medoids/hierarchical/chronological, dirac) vs the PROPOSED
# method, n_rp ∈ {5,10,20,40,80}. Also part of configs/comparison.toml. Investment regime:
# regret fixes invested_units and re-optimizes operations. Only k-means is seed-sensitive.
inputs = ["tyndp/gep"]
master_seed = 382507
n_seeds     = 5
solver      = "gurobi"
inputs_dir  = "../inputs"
outputs_dir = "../outputs"
