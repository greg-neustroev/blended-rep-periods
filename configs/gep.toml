# Generation Expansion Planning case study (also carries the tol/normalization/init sensitivity factorial).
inputs = ["tyndp/gep"]

master_seed = 382507
n_seeds     = 10

solver      = "gurobi"
inputs_dir  = "../inputs"
outputs_dir = "../outputs"
