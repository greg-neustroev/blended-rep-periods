# Sienna 5-bus — held-out comparison (small illustrative storage network). Full clustering ×
# weight matrix (28 combos) at economic, n_rp ∈ {10,20,40,80}; also part of comparison.toml.
# storage_regret day-exact.
inputs = ["sienna/5bus"]
master_seed = 382507
n_seeds     = 5
solver      = "gurobi"
inputs_dir  = "../inputs"
outputs_dir = "../outputs"
