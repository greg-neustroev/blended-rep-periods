# P2X dispatch (European, storage) — held-out comparison, run individually. Full clustering ×
# weight matrix (28 combos) at economic, n_rp ∈ {10,20,40,80}; also part of comparison.toml.
# storage_regret graded day-exact. Only
# k-means/k-medoids are seed-sensitive.
inputs = ["tyndp/p2x"]
master_seed = 382507
n_seeds     = 5
solver      = "gurobi"
inputs_dir  = "../inputs"
outputs_dir = "../outputs"
