# P2X dispatch (European, storage) — held-out comparison (Study 3), run individually.
# Traditional baselines vs the PROPOSED method, n_rp ∈ {5,10,20,40,80}. Also part of
# configs/comparison.toml. Dispatch-only; storage_regret graded at fix_every = 30.
inputs = ["tyndp/p2x"]
master_seed = 382507
n_seeds     = 5
solver      = "gurobi"
inputs_dir  = "../inputs"
outputs_dir = "../outputs"
