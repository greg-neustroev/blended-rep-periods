# Sienna 118-bus (storage, larger non-TYNDP network) — held-out comparison (Study 3), run
# individually. Traditional baselines vs the PROPOSED method, n_rp ∈ {5,10,20,40,80}. Also
# part of configs/comparison.toml. Its ~300 generators make the clustering matrix tall, so
# its runtime breakdown is the most informative among the 365-period systems.
inputs = ["sienna/118bus"]
master_seed = 382507
n_seeds     = 5
solver      = "gurobi"
inputs_dir  = "../inputs"
outputs_dir = "../outputs"
