export run_experiment

function run_experiment(data::ExperimentData, m::JuMP.GenericModel{Float64}, m_eval::JuMP.GenericModel{Float64})
    # TODO: Implement the logic to run the experiment using the provided data and models.
    # This function should execute the optimization model `m` and evaluate it with `m_eval
end
